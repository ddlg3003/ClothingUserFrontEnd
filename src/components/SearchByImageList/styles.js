import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  container: {
    // backgroundColor: '#fff',
    marginBottom: '120px',
  },
  moreButton: {
    display: 'flex',
    justifyContent: 'center',
  },
  image: {
    maxHeight: '300px',
    marginBottom: '20px',
    maxWidth: '200px',
    borderRadius: '10px',
  },
}));
