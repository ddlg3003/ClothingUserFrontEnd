import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  controlWidth: {
    width: '180px',
  },
  select: {
    '& .MuiOutlinedInput-notchedOutline': {
      borderRadius: '20px',
    },
  },
}));
