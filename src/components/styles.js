import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    root: {
        height: '100%',
    },
    toolbar: {
        height: '100px'
    },
    content: {
        flexGrow: 1,
        width: '100%',
    }
}))